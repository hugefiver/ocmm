# v1 Skills and Prompts — Sources, Characteristics, and Maintenance

This document is the single source of truth for v1 skill and prompt provenance, characteristics, and maintenance rules.

Naming note: `v1` remains the configuration value and on-disk version label. Prompt text shown to models calls this the `deepwork` workflow to describe the behavior rather than the version tag.

## Skills Source Mapping

| v1 skill | Upstream | Upstream version | Adjustments | Last synced |
|----------|----------|------------------|-------------|-------------|
| brainstorming | obra/superpowers/brainstorming | v6.1.1+ | removed visual-companion section; removed spec-document-reviewer-prompt reference; replaced skill invocation language to match v1 auto-injected model; step 2 restructured to ambiguity assessment + conditional `clarifier` consultation (skip when clear); step 7 spec approval made conditional (user delegation OR self-review ambiguity check passes); HARD-GATE approval sources expanded to three (user approval / self-review pass / user delegation "你自己决定" / "无需批准自行继续" / "review N 次就下一步"); frontmatter kept as first bytes for OpenCode native slash skill loading. Synced v6.1.1+ visual-companion just-in-time change was NOT applied (v1 fork already removed visual-companion entirely). **2026-07-13 ocmm-native workflow adjustment:** added explicit "First discovery wave" checklist step that occurs before decomposition and planner-trigger decisions; renumbered subsequent checklist steps. | 2026-07-13 |
| writing-plans | obra/superpowers/writing-plans | v6.1.1+ | Removed excluded-skill cross-references; subagent-driven remains the execution path. Added Task Right-Sizing, Global Constraints, Interfaces, and a mandatory current-revision plan-critic loop with `[REJECT]`/`[OKAY]`/`[OKAY-UNAMBIGUOUS]`; timeout/partial/old-plan responses never approve and plan edits invalidate receipts. File-backed plans are reserved for relatively complex work with post-discovery uncertainty or durable coordination. Planning tiers are selected only from current callable availability (`low -> normal` for explicit cost/latency, normal for small/clear, `high -> normal` for complex, `max -> high -> normal` for high-risk), with the `plan-critic-low` xhigh-equivalent floor intact. **2026-07-22 role correction:** Reviewer and Oracle profiles do not review plans; `plan-critic` owns plan review and its receipt, while genuinely difficult/strict/high-risk decision blockers return to the orchestrator. | 2026-07-22 |
| subagent-driven-development | obra/superpowers/subagent-driven-development | v6.1.1+ | Replaced routine per-task spec/code-quality loops with per-agent completion/integration checks plus one final acceptance review; subagents report changes and never perform Git writes. Independent tasks may use parallel implementers when they do not share files/state. Final acceptance uses ordered Oracle slots and logical tiers: simple => first Oracle normal; complex/cross-module => Oracle + Reviewer at high then normal; security/performance/data-loss/release/runtime-safety => max then high then normal; later Oracle slots are added only for intentionally requested evidence and configured profiles never auto-fan-out. Findings use `[product]`/`[evidence]`. **2026-07-22 role correction:** early review requires an actual implementation diff and is limited to implemented-code quality/integration concerns; BLOCKED tasks without code, architecture design, plan defects, and root-cause debugging never use Reviewer/Oracle profiles. | 2026-07-22 |
| requesting-code-review | obra/superpowers/requesting-code-review | v6.0.3 | Integrated final code review only, using a committed range or working-tree/staged diff. Selection has ordered Oracle slots plus independent logical rigor tiers: simple => first Oracle normal; complex/cross-module => Oracle + Reviewer high then normal; security/performance/data-loss/release/runtime-safety => max then high then normal; additional evidence adds later Oracle slots in ordinal order. Configured slots/tiers never auto-fan-out, `reviewer-2nd` does not exist, and xhigh-equivalent floors remain (GPT-5.6 may use native `max`). Findings use `[product]`/`[evidence]`. **2026-07-22 role correction:** Reviewer is primary-model/primary-lane self-review, Oracle slots are external-model cross-checks by default, explicit user configuration may remove heterogeneity, and both are limited to software implementation acceptance or focused code-quality verification after a diff exists. They never review plans. | 2026-07-22 |
| receiving-code-review | obra/superpowers/receiving-code-review | v6.1.1+ | no content changes (skill is self-contained); frontmatter kept as first bytes for OpenCode native slash skill loading. Synced v6.1.1+: upstream changes (de-CLAUDE.md hardcoding, "Circle K" → direct expression) were already absent from this fork's base; no merge required. | 2026-07-03 |
| dispatching-parallel-agents | obra/superpowers/dispatching-parallel-agents | v6.1.1+ | frontmatter kept as first bytes for OpenCode native slash skill loading. Synced v6.1.1+: platform-agnostic dispatch pseudocode (TS `Task()` → text `Subagent (general-purpose)` + parallel/sequential dispatch rule). Clarified "Review and Integrate" and "Verification" sections to distinguish completion/integration check from full reviewer loop, require summary/evidence/touched-files/diff/conflict/targeted-test checks, and note that final acceptance review is performed by the parent workflow after all tasks are integrated. | 2026-07-06 |

**Note on injection (2026-06-29):** Only `brainstorming` is injected into the system message (HARD-GATE protection). The other 5 v1 skills are registered as slash commands and loaded on demand. `V1_INJECTED_SKILLS = ["brainstorming"]`; `V1_COMMAND_SKILLS` = all 6. Deepwork prompts include a Skill Reference table guiding on-demand loading.

**Shared skill (not v1):** `remove-ai-slops` at `skills/remove-ai-slops/SKILL.md` — simplified fork from omo's `shared-skills/remove-ai-slops` (317→~150 lines, generalized from Python-specific). Auto-discovered by `loadSharedSkills`, registered as `/remove-ai-slops`. Not injected into system message.

**Shared skill (not v1):** `using-git-worktrees` at `skills/using-git-worktrees/SKILL.md` — adapted from obra/superpowers v6.1.1+ `using-git-worktrees`. Adjustments: consent gate (activates ONLY on explicit user request, never auto-trigger); bash→pwsh syntax (`git rev-parse --git-dir`/`--git-common-dir`, `Test-Path`); removed OpenCode-inapplicable native worktree tool step (Step 1a); pnpm-focused project setup (ocmm uses pnpm); sibling-placement naming convention. Auto-discovered by `loadSharedSkills`, registered as `/using-git-worktrees`. Last synced 2026-07-03.

**Shared skill (not v1):** `debugging` at `skills/debugging/` — forked from omo's `shared-skills/debugging` (hypothesis-driven loop, journal-then-modify, red-green TDD fix, manual QA, artifact scrub), adapted for PowerShell/uutils and local `lsp_*`/`ast-grep`/`rg`/`fd` routing. **2026-07-22 escalation correction:** after two failed evidence rounds, one `hard-reasoning` escalation is allowed only when the root-cause hunt is genuinely difficult; Reviewer/Oracle profiles are prohibited for debugging and remain reserved for software implementation acceptance or focused code-quality verification. Non-debug artifact facts use bounded `research`. Auto-discovered by `loadSharedSkills`. Synced 2026-07-06 from `./omo@a7ac217`, including the local structured-logging bridge in `references/logging.md` and `references/methodology/06-fix.md`.

**Shared skill (not v1):** `frontend` at `skills/frontend/` — adapted from omo's `shared-skills/frontend` (three-ruleset router: design + perfection + ui-ux-db). ocmm carries only the §5 project-original design docs (`README.md`, `_INDEX.md`, `aside.md`, `design-system-architecture.md`, `react-dev-tooling-skill.md`) + `references/perfection/*`; third-party taste skills, brand refs, ui-ux-db data, and the `designpowers/` integration layer are DMCA-safe submodule-only in omo and are NOT vendored (ocmm routes to the local `open-design` skill instead). ocmm `SKILL.md` keeps the "three rulesets" wording (NOT synced to upstream's "four rulesets" — ocmm has no designpowers ruleset). Synced 2026-07-06 from `./omo@a7ac217`: added `references/design/aside.md` (§5 project-original Aside brand design system reference, self-contained) and `references/design/clone-from-url.md` (§5 project-original runtime `getComputedStyle` extraction workflow). `SKILL.md` updated: 69→70 brand count (description + body + Layer B list added `aside`), 81→83 cataloged files, `_INDEX.md` and `README.md` counts synced (69→70 brand, 7→8 Developer Tools, aside.md row + "AI browser / agentic browser" mood shortcut), `ATTRIBUTION.md` §4 provenance added, added "Aside-style agent-browser landing" and "Clone this site / rebuild `<url>`" quick routes. Known gap: `clone-from-url.md` references `/visual-qa` reference-fidelity mode in Phase 4 + outcome — ocmm does NOT have the `visual-qa` skill; the methodology (getComputedStyle extraction, DESIGN.md contract, reusable primitives) stands without it, but the QA gate falls back to manual cross-breakpoint screenshot comparison.

## Codex Plugin Prompts (`prompts/codex/`)

The Deepwork Codex plugin uses a dedicated `prompts/codex/` directory (parallel to `prompts/v1/` and `prompts/omo/`). These are v1 prompts adapted for the Codex environment:

- **Tool substitutions**: `task()` → `multi_agent_v1.spawn_agent`, TodoWrite → `update_plan`, Edit/Write → `apply_patch`, `lsp_diagnostics` → LSP via lsp MCP.
- **Skill Reference**: slash commands (`/writing-plans`) → load by name (`deepwork-writing-plans`).
- **Brainstorming injection**: brainstorming SKILL.md is embedded into each agent's `developer_instructions` at packaging time (Codex has no runtime system message injection).
- **Codex Environment note**: deepwork/gpt.md includes a Codex-specific compatibility section.
- **Planner proxy-loophole sync (2026-07-06)**: `prompts/codex/deepwork/planner.md` mirrors the omo+v1 planner update closing the implement-by-proxy loophole (no subagent is an execution worker). Synced from `./omo@a7ac217` Prometheus prompt.
- **GPT-5.6 compact calibration (2026-07-19)**: `prompts/codex/deepwork/gpt-5.6.md` shares the v1/omo four-section doctrine, keeps the Codex `<deepwork-mode>` wrapper, says profiles may carry the layer ahead of runtime selection, and names embedded skills plus Codex tool-compatibility rules as authoritative. Generated `dw-*` profiles must contain outcome/waiting/revalidation guidance without reintroducing removed headings inside the specialization segment. **2026-07-22 agile calibration and role correction:** GPT-5.6 assesses complexity and rigor before workflow selection, avoids forcing low-complexity work through full engineering or non-triggered skill ceremony, delegates only for context savings or workflow/parallel-delivery value, treats Reviewer as primary-lane implementation self-review and Oracle as external-model implementation cross-check, reserves both for implementation acceptance/code quality, and considers parallel subagents for independent non-coupled modules.
- **Codex prompt policy refresh (2026-07-20)**: `prompts/codex/deepwork/codex.md` mirrors the v1/omo changed-input validation cadence and evidence-bounded delegation contract while retaining Codex-native dispatch/session wording. This source policy is distinct from `src/codex/plugin-generator.ts` MultiAgent V1/V2 compatibility; current generated profiles do not consume this `codex.md`, so regeneration must produce no tracked bundle delta.
- `loadAdapterConfig` forces `workflow: "codex"` for Deepwork plugin packaging regardless of source config.
- Session resume via `task_id` is not supported in Codex (noted as limitation in gemini.md, codex.md).

### Skill Template Files

| v1 file | Upstream file | Adjustments |
|---------|---------------|-------------|
| subagent-driven-development/implementer-prompt.md | superpowers/subagent-driven-development/implementer-prompt.md | local flat-workflow boundary: direct tools first; only permitted utility leaves; no planner/reviewer/peer implementation dispatch; no subagent Git writes; verified evidence returns to the orchestrator. **2026-07-18 grammar-wide review-profile wording:** the Delegation Boundary "Do not launch" clause now covers every Reviewer profile (`reviewer`, `reviewer-low`, `reviewer-high`, `reviewer-max`) and every Oracle profile (`oracle`, `oracle-2nd`, configured `oracle-3rd`…`oracle-9th`, plus `low`/`high`/`max` tier variants) instead of the stale `oracle`/`oracle-high` enumeration. |
| subagent-driven-development/spec-reviewer-prompt.md | superpowers/subagent-driven-development/spec-reviewer-prompt.md | retained only for optional narrow consultations; not a routine per-task gate |
| subagent-driven-development/code-quality-reviewer-prompt.md | superpowers/subagent-driven-development/code-quality-reviewer-prompt.md | retained only for optional narrow quality consultations after completion/integration checks; no longer requires prior spec-reviewer pass |
| requesting-code-review/code-reviewer.md | superpowers/requesting-code-review/code-reviewer.md | local flat-workflow review input: accepts committed ranges or working-tree/staged diffs and must ask the orchestrator for missing diff evidence instead of asking implementation subagents to commit |
| writing-plans/plan-document-reviewer-prompt.md | superpowers/writing-plans/plan-document-reviewer-prompt.md | extended: added Cross-Task Consistency, Global Constraints, and Plan vs Review Rubric check categories (absorbs upstream Pre-Flight plan review into plan-critic) |

## Prompt Source Mapping

| v1 prompt | Skills referenced | Kept from omo | Dropped from omo | Adapted for v1 |
|-----------|-------------------|---------------|------------------|-----------------|
| deepwork/default.md | brainstorming (injected); 5 on-demand | local ocmm agent/category structure, exact-scope and evidence discipline | upstream CODE RED bulk and model-specific ceremony | concise local deepwork controller; no visible version label; output discipline (incremental file writes, with anti-minimal-segment clause so segments stay complete); shell-adaptation guidance treats prompt/skill shell snippets as examples to translate to the active runtime shell; on-demand Skill Reference table (brainstorming only injected, others via slash command); anti-slop checklist; omo-aligned scope_constraints; requesting-code-review trigger says all implementation tasks complete / major feature / before merge. **2026-07-13 ocmm-native workflow adjustment:** added Discovery Before Planning, Planner Trigger (complexity/coordination-based, not step-count), Answer-When-Answerable, full-request scope (no default MVP), and `[product]`/`[evidence]` review labels. |
| deepwork/gpt.md | brainstorming (injected); 5 on-demand | upstream GPT structured-instruction adaptation, certainty protocol, TDD/QA/reviewer gate | upstream-only agent/tool names | local agent names; shell-adaptation guidance treats prompt/skill shell snippets as examples to translate to the active runtime shell; on-demand Skill Reference (brainstorming injected, others slash command); anti-slop checklist; omo-aligned scope_constraints; task-tiered decision framework (Simple/Moderate/Complex) with clarity gate; batch processing instruction including explicit instruction to avoid per-subtask full reviewer loops and use final acceptance review after all tasks; tier-dependent TDD/scenario/QA. **2026-07-13 ocmm-native workflow adjustment:** added Discovery Before Planning, Planner Trigger (complexity/coordination-based, not step-count), Answer-When-Answerable, full-request scope (no default MVP), and `[product]`/`[evidence]` review labels. |
| deepwork/gpt-5.6.md | omo GPT-5.6 Hephaestus prompt doctrine plus current Momus review calibration | outcome-first completion, conditional retrieval/delegation, evidence-first reporting, native `max`, and authorization priority | Additive four-section GPT-5.6 calibration, capped at 3,500 characters and 60% of its baseline. Shared doctrine comes from effective base/role/category/skill layers. **2026-07-22 agile calibration and role correction:** adds complexity/rigor-first process scaling, context/workflow-gated subagent use, parallel implementation for independent non-coupled modules, Reviewer primary-lane self-review, Oracle external-model cross-checks, and an implementation-acceptance/code-quality-only review boundary. |
| deepwork/gemini.md | brainstorming (injected); 5 on-demand | upstream Gemini intent gate, tool mandate, delegation and QA strictness | upstream-only agent/tool names | local agent names; shell-adaptation guidance treats prompt/skill shell snippets as examples to translate to the active runtime shell; on-demand Skill Reference; anti-slop checklist; omo-aligned scope_constraints. **2026-07-13 ocmm-native workflow adjustment:** added Discovery Before Planning, Planner Trigger (complexity/coordination-based, not step-count), Answer-When-Answerable, full-request scope (no default MVP), and `[product]`/`[evidence]` review labels. |
| deepwork/glm.md | brainstorming (injected); 5 on-demand | upstream GLM 5.x calibration, deep-first thinking with thinking-channel mandate, evidence-first completion | upstream-only agent/tool names, shallow-default thinking_depth, fable_counters that suppress planning | local agent names; shell-adaptation guidance treats prompt/skill shell snippets as examples to translate to the active runtime shell; on-demand Skill Reference; anti-slop checklist; omo-aligned scope_constraints; thinking-channel-first reasoning with visible-reasoning-surfacing clause (thinking channel holds full deliberation, visible output may surface the key reasoning chain), softened fable_counters to allow 3+ step planning, output discipline (incremental file writes, with anti-minimal-segment clause so segments stay complete). **2026-07-13 ocmm-native workflow adjustment:** added Discovery Before Planning, Planner Trigger (complexity/coordination-based, not step-count), Answer-When-Answerable, full-request scope (no default MVP), and `[product]`/`[evidence]` review labels. |
| deepwork/codex.md | brainstorming (injected); 5 on-demand | upstream Codex tier triage, success criteria, RED/GREEN/SURFACE loop, cleanup/review gates, TUI visual QA evidence requirement | Codex harness-only `multi_agent_v1`, `fork_context`, TOML routing, `update_plan`/`create_goal`, Sparkshell-only command lens | OpenCode/ocmm task/todowrite semantics; shell-adaptation guidance replaces local PowerShell command guidance and requires translating examples to the active runtime shell; on-demand Skill Reference; anti-slop checklist; omo-aligned scope_constraints; requesting-code-review trigger says all implementation tasks complete / major feature / before merge; old LIGHT/HEAVY verification gate and reviewer-loop wording collapsed into the shared completion/integration-check plus final-acceptance-review cadence. **2026-07-13 ocmm-native workflow adjustment:** added Discovery Before Planning, Planner Trigger (complexity/coordination-based, not step-count), Answer-When-Answerable, full-request scope (no default MVP), and `[product]`/`[evidence]` review labels. **2026-07-20 validation/delegation refresh:** per-increment verification now runs only touched tests and affected scenarios; broader suite/typecheck/build gates rerun only after relevant input changes, followed by one final full pass. HEAVY RED/GREEN/SURFACE/CLEAN and final acceptance remain mandatory. Delegations preserve the local envelope and add `GOAL`/`STOP WHEN`/`EVIDENCE`, parent evidence verification, and child-stop versus whole-goal parent-stop separation. |
| deepwork/planner.md | writing-plans | upstream planner doctrine from ultrawork planner prompt | Prometheus branding and upstream skill name | Local planner naming, shell adaptation, discovery-before-planning, complexity/coordination trigger, full-request scope, and implement-by-proxy prohibition. Planner discovery may use only read-only utility leaves; it never dispatches plan-critic, Reviewer/Oracle profiles, implementation agents, or decision agents. Genuinely difficult/strict/high-risk decision blockers return to the orchestrator for optional `hard-reasoning`. |
| agents/orchestrator.md | all 6 | upstream Sisyphus orchestration structure and intent verbalization gate | lore/brand-heavy identity, upstream-only tool names | Local role/category routing, intent verbalization, mandatory skill triggers, tool selection, parallel dispatch, subagent Git restrictions, completion/integration checks, and committed-range or working-tree review input. Orchestrator exclusively owns workflow-agent composition, callable-profile tier selection, review fan-out, and difficult-decision routing. **2026-07-22 role correction:** Reviewer is primary-lane implementation self-review; Oracle slots are external-model cross-checks; ordinary architecture/debugging stays direct or in the debugging workflow, and only genuinely difficult/strict/high-risk decisions use `hard-reasoning`. |
| agents/reviewer.md | requesting-code-review | upstream Oracle advisor role | Oracle branding, upstream-only restrictions | Shared read-only implementation-review contract: `reviewer` is primary-model or primary-lane self-review; Oracle slots are external-model cross-checks by default and explicit user configuration may remove heterogeneity. Both are limited to software implementation acceptance or focused code-quality verification after a diff exists, never research, ideation, pre-implementation architecture, root-cause debugging, general-answer validation, or routine confidence. Ordered Oracle priority, `[APPROVED]`/`[REJECTED]` verdicts, and bounded leaf read-only evidence lookup remain; review profiles never nest. |
| agents/planner.md | writing-plans | upstream Prometheus planner scope | `.omo`-specific plan-only command flow | Local plan path, writing-plans contract, brainstorm precondition, tool guidance, parallel lookup, and exact current-revision plan-critic receipt semantics. Planner defaults to direct planning with leaf read-only fact gathering; it never dispatches plan-critic, Reviewer/Oracle profiles, implementation agents, decision agents, or routine review checks. Genuinely difficult/strict/high-risk decision blockers return to the orchestrator. |
| agents/clarifier.md | brainstorming, writing-plans | upstream Metis pre-planning analysis | Metis branding, `call_omo_agent`, Prometheus-only handoff | Local clarifier naming, full-outcome scope/exclusions, direct evidence, and leaf read-only discovery only. It never delegates intent classification or final questions, never routes pre-implementation architecture to Reviewer/Oracle profiles, and reports only genuinely difficult/strict/high-risk decision blockers for orchestrator-owned `hard-reasoning`. |
| agents/plan-critic.md | writing-plans, requesting-code-review | upstream Momus blocker-focused review | Momus branding, `.omo/plans`-only extraction | local `plan-critic` name, inline-or-file plan review; three-state verdict (`[REJECT]`/`[OKAY]`/`[OKAY-UNAMBIGUOUS]`) with ambiguity assessment dimension; `[OKAY-UNAMBIGUOUS]` authorizes skipping user plan approval; the current `plan-critic` receipt covers exactly one complete, current plan revision; any plan edit invalidates that receipt and requires a fresh review; **2026-07-18 bounded nested-delegation adjustment:** plan-critic uses direct file/search first, may request only leaf read-only lookup to verify one concrete plan claim, and never delegates the receipt verdict |
| category/frontend.md | (deepwork injected separately) | full omo frontend category constraints | old shortened category router | strongly aligned category role; shell-adaptation guidance included directly because category prompts may run without a deepwork layer; no visible version label |
| category/creative.md | (deepwork injected separately) | full omo creative category constraints | old shortened category router | strongly aligned category role; shell-adaptation guidance included directly because category prompts may run without a deepwork layer; no visible version label |
| category/hard-reasoning.md | (deepwork injected separately) | full omo hard-reasoning category constraints | old shortened category router | Genuinely difficult, strict, or high-risk decision-only analysis; ordinary architecture, first-line debugging, routine design choice, or scale alone do not qualify. Shell adaptation is included directly because category prompts may run without a deepwork layer. |
| category/research.md | (deepwork injected separately) | full omo research category constraints | old shortened category router | strongly aligned category role; shell-adaptation guidance included directly because category prompts may run without a deepwork layer; no visible version label |
| category/quick.md | (deepwork injected separately) | full omo quick category constraints | old shortened category router | fully specified mechanical edits; strongly aligned category role; shell-adaptation guidance included directly because category prompts may run without a deepwork layer; no visible version label |
| category/coding.md | (deepwork injected separately) | bounded-code-edit category constraints | old shortened category router and vague effort label | determined code edits and bug fixes only; strongly aligned category role; shell-adaptation guidance included directly because category prompts may run without a deepwork layer; no visible version label |
| category/normal-task.md | (deepwork injected separately) | new local ordinary-task category | old vague effort category split | ordinary bounded tasks with known acceptance criteria; shell-adaptation guidance included directly because category prompts may run without a deepwork layer; no visible version label |
| category/complex.md | (deepwork injected separately) | new local coordinated-task category | old vague effort category split | coordinated multi-step ordinary tasks; shell-adaptation guidance included directly because category prompts may run without a deepwork layer; no visible version label |
| category/deep.md | (deepwork injected separately) | upstream deep category semantics | old shortened category router and vague effort label | autonomous system development and feature delivery loop; strongly aligned category role; shell-adaptation guidance included directly because category prompts may run without a deepwork layer; no visible version label |
| category/documenting.md | (deepwork injected separately) | prose and documentation discipline | old `writing` category name | standalone documentation/prose that does not change product behavior; shell-adaptation guidance included directly because category prompts may run without a deepwork layer; no visible version label |

## Flat Workflow Subagent Policy (2026-07-17)

- Utility leaves are `quick`, `code-search`, `explore`, `doc-search`, `research`, and `media-reader`; they never dispatch.
- Standard workflow subagents may call only utility leaves. Read-only workflow agents exclude `quick` and may call only read-only utility leaves. Planner returns genuinely difficult/strict/high-risk decision blockers to the orchestrator rather than dispatching decision or review agents.
- `deep` and `complex` may additionally call only `coding`, `frontend`, `hard-reasoning`, `creative`, and `documenting` for materially useful bounded deliverables.
- Planner and review roles return plans/findings to their caller. Reviewer/Oracle profiles are implementation-acceptance/code-quality-only; formal planner dispatch, difficult-decision routing, the plan-critic loop, and final acceptance review remain orchestrator-owned.
- Final review accepts either an orchestrator-owned committed range or an uncommitted working-tree/staged diff; implementation subagents do not create commits merely to make review possible.
- `prompts/v1/agents/planner.md`, effective terminal delegation contracts, `skills/v1/subagent-driven-development/SKILL.md`, `skills/v1/subagent-driven-development/implementer-prompt.md`, and `skills/v1/requesting-code-review/SKILL.md` carry exact role permissions and ownership. `prompts/v1/deepwork/gpt-5.6.md` carries only the conservative model-level threshold; Codex generated profiles and copied skills are refreshed from those sources.

## GPT-5.6 Prompt Simplification (2026-07-19)

- Authority: `code-yeongyu/oh-my-openagent` `dev@e8d842a38a7e0ed3edd5fc74f88247f8b63075ad`; reviewed Hephaestus OpenCode/Codex sources and the GPT-5.6 Momus source.
- Evidence: PR #6012 and #6010 favor shorter role-preserving prompts; PR #6100 requires observable `GOAL` / `STOP WHEN` / `EVIDENCE`; PR #6151 avoids empty polling and unchanged-input revalidation.
- v1 difference: retains one `<deepwork-mode>` wrapper and makes injected skills authoritative.
- Codex difference: retains one wrapper, guards ahead-of-runtime carriage for non-GPT-5.6 models, and makes embedded skills plus Codex tool-compatibility rules authoritative.
- Shared body: outcome-first completion, conservative retrieval/delegation, context-efficient waiting/revalidation, and reporting priority. Generic workflow and exact permission doctrine remain in their existing authoritative layers.
- 2026-07-22 agile calibration: the shared body now first scales process to task complexity and required rigor; low-complexity work avoids full engineering and non-triggered Superpowers ceremony. Subagents require parent-context savings or workflow/parallel-delivery value, reviewer/Oracle profiles are limited to implementation acceptance or focused code-quality verification and selected by existing rules, and independent non-coupled modules may be implemented through parallel subagents.

## Shared Characteristics

All v1 prompts share these principles (derived from superpowers skills):

1. **5-phase chain**: brainstorm -> plan -> implement -> completion/integration check -> final acceptance review -> receive-review
2. **TDD cycle**: write failing test -> run -> implement -> run -> commit
3. **Review cadence**: per-agent completion/integration check after each returned implementer; one unified final acceptance review after all plan tasks complete; full spec/code-quality reviewer loop is not run automatically after every subtask
4. **Fresh subagent per task**: each implementation task gets a clean subagent
5. **No performative agreement**: no "You're right!", no "Great point!"
6. **No placeholders**: plans must have zero TODO/TBD/placeholder
7. **Bite-sized tasks**: 2-5 minute steps in plans
8. **Investigate before claiming**: never speculate about unread code
9. **Parallelize reads**: batch independent file reads
10. **Deepwork tag envelope**: every `prompts/v1/deepwork/*.md` file is wrapped in `<deepwork-mode>` to match the omo prompt envelope while keeping `v1` only as the config/path label.
11. **No model-visible version label**: files under `prompts/v1/` must not mention `v1` to the model. Use `deepwork` wording in model-facing prompt text.
12. **Concrete category scope**: category prompts describe the work shape and deliverable they handle, not vague model strength or weak/strong capability levels. Local mapping: `hard-reasoning` covers ultrabrain-style decisions, `deep` covers autonomous system development and feature delivery, `coding` covers determined code edits and bug fixes, `normal-task` covers bounded fallback work with known acceptance criteria, `complex` covers coordinated multi-step or cross-cutting ordinary tasks that remain below autonomous feature delivery, and `documenting` covers standalone text.
13. **Reasoning defaults**: built-in category defaults from `coding` upward should use `max` in the local variant vocabulary. GPT-5.6 supports native `max`; other families use their selected model's maximum-effort control when available. `quick` remains lightweight. User-declared model, variant, and provider parameters are respected as explicit configuration except for review/plan-review floors.
14. **Flat delegation graph**: effective prompt contracts and granular task permissions terminate at utility leaves; planner returns difficult-decision blockers to the orchestrator, and no role transfers formal review ownership away from the orchestrator.

## Maintenance Rules

1. v1 skill file changes (in `skills/v1/`) MUST update the Skills Source Mapping table in this doc in the same commit.
2. v1 prompt file changes (in `prompts/v1/`) MUST update the Prompt Source Mapping table in this doc in the same commit.
3. Source Mapping table changes MUST be reflected in the actual files in the same commit.
4. Adding a new v1 file requires adding a row to the relevant table.
5. Removing a v1 file requires removing its row and updating any references in `src/intent/skill-loader.ts` or `src/intent/prompt-loader.ts`.
6. Upstream skill sync requires updating the "Last synced" date and "Upstream version" column, plus re-applying any adjustments listed.
7. omo prompts (`prompts/omo/`) are tracked in `docs/prompt-sync.md`, not this v1 derivative table. Update that doc when omo prompt files change.

## Upstream Sync Procedure

When syncing a v1 skill from upstream superpowers:

1. Read the current upstream `SKILL.md` from `~/.config/opencode/skills/superpowers/<name>/SKILL.md`
2. Diff against the current `skills/v1/<name>/SKILL.md`
3. Re-apply the adjustments listed in the Skills Source Mapping table
4. Update the "Last synced" date and "Upstream version" in this doc
5. Run `pnpm test` to verify nothing broke
6. Commit with message: `chore: sync v1/<name> skill from superpowers <version>`
