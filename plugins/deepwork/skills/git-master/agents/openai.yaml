interface:
  display_name: "git-master (ocmm)"
  short_description: "Atomic Git commits, history surgery, and source archaeology"
  default_prompt: "Use $git-master to inspect the repository state, preserve unrelated work, and handle the requested Git operation with atomic grouping, history safety checks, and evidence-backed verification."
